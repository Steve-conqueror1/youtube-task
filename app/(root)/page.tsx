import React from "react";
import SearchBox from "@/components/SearchBox";

const Home = () => {
    return (
        <section className="container-height container w-full flex flex-col justify-center items-center">
            <SearchBox/>
        </section>
    )
};

export default Home;